machine:
  services:
    - docker

deployment:
  hub:
    branch: master
    commands:
      - docker login  -u $quay_user -p $quay_pass -e $quay_email quay.io
      - sbt "project service" docker:publish